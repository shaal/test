id: towerhealth_providers
label: Import tower health provider content
migration_groups:
  - TowerHealth MSOW import
dependencies:
  enforced:
    module:
      - towerhealth_msow_migration

source:
  plugin: 'csv'
  # Full path to the file.
  path: 'modules/custom/towerhealth_msow_migration/data/tbl_Dir_Practitioner.csv'
  # Column delimiter. Comma (,) by default.
  header_row_count: 1
  ids: [practioner_id]
  # Here we identify the columns of interest in the source file.
  # Each numeric key is the 0-based index of the column.
  # For each column, the key below is the field name assigned to
  # the data on import, to be used in field mappings below.
  # The value is a user-friendly string for display by the
  # migration UI.
  track_changes: true
  fields:
    - name: practioner_id
      label: 'Practioner ID'
    - name: last_name
      label: 'Last name'
    - name: first_name
      label: 'Last name'
    - name: middle_name
      label: 'Middle name'
    - name: professional_title
      label: 'Professional Title'
    - name: gender
      label: 'Gender'
    - name: app
      label: 'APP'
    - name: employment_status
      label: 'Employment Status'
    - name: npi_id
      label: 'NPI ID'

process:
  field_profile_first_name: first_name

  field_profile_last_name: last_name

  field_profile_middle_name: middle_name

  field_profile_gender:
    - plugin: default_value
      default_value: 'female';
    - plugin: static_map
      source: gender
      map:
        M: male
        F: female

  field_profile_practid: practioner_id

  field_profile_npi: npi_id

  field_profile_professional_title: professional_title

  _prepare_type:
    - plugin: static_map
      source: employment_status
      map:
        'employed': 'employed'
        'contracted': 'contracted'
        '': 'independent'

  field_profile_type:
    - plugin: default_value
      default_value: 'tower_health_employed';
    - plugin: static_map
      source:
        - app
        - '@prepare_type'
      map:
        false:
          employed: tower_health_employed
          independent: independent
          contracted: contracted
        true:
          employed: app_employed
          independent: independent
          contracted: contracted

  type:
    plugin: default_value
    default_value: profile

  moderation_state:
    plugin: default_value
    default_value: published

destination:
  plugin: 'node_merge'
  lookup_field: 'field_profile_practid'
